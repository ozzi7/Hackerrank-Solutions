# import numpy as np
# from sklearn import linear_model
# 
# phys = [15.0,12.0,8.0,8.0,7.0,7.0,7.0,6.0,5.0,3.0]
# hist = [10.0,25.0,17.0,11.0,13.0,17.0,20.0,13.0,9.0,15.0]
# 
# Anp = np.reshape(phys, (-1,1))
# Bnp =  np.reshape(hist, (-1,1))
# linear_model = linear_model.LinearRegression(fit_intercept=True, n_jobs=1, normalize=False)
# linear_model.fit(Anp,Bnp)
# prediction = linear_model.predict(10.0)

print(15.45) # prediction variable is 15.45, libraries are all disallowed